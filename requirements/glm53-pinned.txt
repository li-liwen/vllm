# glm53-gfx908 pinned dependency additions
# Base image (btbtyler09/vllm-rocm-gfx908@sha256:03f325eb...) already satisfies all
# other common/rocm requirements per the audit in deploy/glm53-gfx908/build/.
# torch 2.12.0+git6bbd260 / triton 3.7.1+gitf0b55c07 / transformers 5.16.1 are
# installed in the base image and MUST NOT be touched; we install with --no-deps.
tilelang==0.1.10
apache-tvm-ffi==0.1.10
